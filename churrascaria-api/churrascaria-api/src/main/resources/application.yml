spring:
  application:
    name: Pedido API
  datasource:
    url: jdbc:h2:mem:pedido
    username: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console